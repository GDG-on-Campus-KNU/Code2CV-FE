import { button } from './button';
import { color, combinationColor } from './color';
import { fontSize } from './fontSize';

export const vars = {
    color,
    combinationColor,
    fontSize,
    button,
};
