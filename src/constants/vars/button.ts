import { fontSize } from './fontSize';

export const button = {
    small: `${fontSize.small}; padding: 6px 12px`,
    medium: `${fontSize.medium}; padding: 11px 80px`,
    large: `${fontSize.large}; padding: 15px 168px`,
};
