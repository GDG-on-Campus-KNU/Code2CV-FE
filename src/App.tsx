import type { FC } from 'react';
import { HomePage } from './components/home/pages/Homepage';

export const App: FC = () => {
    return (
        <div>
            <HomePage />
        </div>
    );
};
